apiVersion: v1
kind: Service
metadata:
  name: excalidraw
  annotations:
    external-dns.alpha.kubernetes.io/hostname: excalidraw.lan
spec:
  selector:
    app: excalidraw
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: LoadBalancer
  loadBalancerIP: 192.168.1.27