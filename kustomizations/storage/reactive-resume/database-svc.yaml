apiVersion: v1
kind: Service
metadata:
  name: reactive-resume-postgresql
  labels:
    app.kubernetes.io/name: reactive-resume-postgresql
spec:
  type: ClusterIP
  ports:
  - port: 5432
    targetPort: 5432
  selector:
    app.kubernetes.io/name: reactive-resume-postgresql
