apiVersion: v1
kind: Service
metadata:
  name: wikijs-postgresql
  labels:
    app.kubernetes.io/name: wikijs-postgresql
spec:
  type: ClusterIP
  ports:
  - port: 5432
    targetPort: 5432
  selector:
    app.kubernetes.io/name: wikijs-postgresql
