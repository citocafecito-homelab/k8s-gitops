apiVersion: apps/v1
kind: Deployment
metadata:
  name: transmission
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: transmission
      app.kubernetes.io/name: transmission
  template:
    metadata:
      labels:
        app: transmission
        app.kubernetes.io/name: transmission
        app.kubernetes.io/part-of: streaming-stack
      annotations:
        app.kubernetes.io/documentation: "https://docs.linuxserver.io/images/docker-transmission/"
        vault.hashicorp.com/agent-inject: "true"
        vault.hashicorp.com/role: vault
        vault.hashicorp.com/agent-inject-secret-transmission: "secret/data/transmission"
        vault.hashicorp.com/agent-inject-template-transmission: |
          {{- with secret "secret/data/transmission" -}}
          {{ .Data.data.USERNAME | writeToFile "/vault/secrets/user" "vault" "1000" "0640" }}
          {{ .Data.data.PASSWORD | writeToFile "/vault/secrets/password" "vault" "1000" "0640" }}
          {{- end -}}

          {{- with secret "secret/data/ntfy" -}}
          {{ .Data.data.ADMIN_TOKEN | writeToFile "/vault/secrets/ntfy" "vault" "1000" "0640" }}
          {{- end -}}
    spec:
      dnsPolicy: ClusterFirst
      serviceAccountName: transmission
      containers:
      - name: gluetun
        image: qmcgaw/gluetun:latest
        env:
        # Permitir entrada al puerto de la WebUI y subredes locales
        - name: FIREWALL_INPUT_PORTS
          value: "8080"
        - name: FIREWALL_OUTBOUND_SUBNETS
          value: "192.168.1.0/24,10.244.0.0/16,10.96.0.0/12"

        # Autenticación PIA desde Secrets
        - name: OPENVPN_USER
          valueFrom:
            secretKeyRef:
              name: pia-credentials
              key: VPN_SERVICE_USER
        - name: OPENVPN_PASSWORD
          valueFrom:
            secretKeyRef:
              name: pia-credentials
              key: VPN_SERVICE_PASSWORD

        # Configuración PIA y Port Forwarding
        - name: VPN_SERVICE_PROVIDER
          value: "private internet access"
        - name: VPN_TYPE
          value: "openvpn"
        - name: OPENVPN_PROTOCOL
          value: "udp"
        - name: PRIVATE_INTERNET_ACCESS_OPENVPN_ENCRYPTION_PRESET
          value: "normal"
        - name: PORT_FORWARD_ONLY
          value: "true"
        - name: VPN_PORT_FORWARDING
          value: "on"
        - name: VPN_PORT_FORWARDING_STATUS_FILE
          value: "/config/forwarded_port"
        resources:
          requests:
            cpu: 50m
            memory: 64Mi
          limits:
            cpu: 200m
            memory: 128Mi
        securityContext:
          runAsUser: 0
          capabilities:
            add:
            - NET_ADMIN
            - NET_RAW
        volumeMounts:
        - name: config
          mountPath: /config
        - name: tun
          mountPath: /dev/net/tun

      - name: transmission
        image: lscr.io/linuxserver/transmission:latest
        ports:
        - containerPort: 9091
          name: webui
          protocol: TCP
        env:
        - name: DOCKER_MODS
          value: ""
        - name: DISABLE_IPTABLES
          value: "true"
        - name: "FILE__USER"
          value: "/vault/secrets/user"
        - name: "FILE__PASS"
          value: "/vault/secrets/password"
        # Base Config
        - name: TRANSMISSION_DOWNLOAD_DIR
          value: "/downloads"
        # Automation
        - name: TRANSMISSION_SCRIPT_TORRENT_ADDED_ENABLED
          value: "true"
        - name: TRANSMISSION_SCRIPT_TORRENT_ADDED_FILENAME
          value: "/scripts/added.sh"
        - name: TRANSMISSION_SCRIPT_TORRENT_DONE_ENABLED
          value: "true"
        - name: TRANSMISSION_SCRIPT_TORRENT_DONE_FILENAME
          value: "/scripts/finished.sh"
        - name: TRANSMISSION_SCRIPT_TORRENT_DONE_SEEDING_ENABLED
          value: "true"
        - name: TRANSMISSION_SCRIPT_TORRENT_DONE_SEEDING_FILENAME
          value: "/scripts/completed.sh"
        # Ratios & Queue
        - name: TRANSMISSION_INCOMPLETE_DIR_ENABLED
          value: "false"
        - name: TRANSMISSION_RENAME_PARTIAL_FILES
          value: "false"
        - name: TRANSMISSION_RATIO_LIMIT
          value: "3.0"
        - name: TRANSMISSION_RATIO_LIMIT_ENABLED
          value: "true"
        - name: TRANSMISSION_DOWNLOAD_QUEUE_ENABLED
          value: "false"
        - name: TRANSMISSION_SEED_QUEUE_ENABLED
          value: "false"
        - name: TRANSMISSION_DOWNLOAD_QUEUE_SIZE
          value: "999"
        - name: TRANSMISSION_SEED_QUEUE_SIZE
          value: "999"
        # Networking
        - name: TRANSMISSION_PEER_PORT_RANDOM_ON_START
          value: "true"
        - name: TRANSMISSION_PEER_LIMIT_PER_TORRENT
          value: "20"
        envFrom:
        - configMapRef:
            name: streaming-stack-config
        resources:
          requests:
            cpu: 50m
            memory: 256Mi
          limits:
            cpu: 200m
            memory: 512Mi
        securityContext:
          capabilities:
            add:
            - NET_ADMIN
            - NET_RAW
        volumeMounts:
        - name: scripts
          mountPath: /custom-cont-init.d/01-setup-config.sh
          subPath: 01-setup-config.sh
        - name: scripts
          mountPath: /scripts
        - name: config
          mountPath: /config
        - name: downloads
          mountPath: /downloads
      nodeSelector:
        kubernetes.io/hostname: catimore
      securityContext:
        fsGroup: 1000
      volumes:
      - name: tun
        hostPath:
          path: /dev/net/tun
      - name: scripts
        configMap:
          name: transmission-scripts
          defaultMode: 0755
      - name: config
        hostPath:
          path: /mnt/transmission
          type: DirectoryOrCreate
      - name: watch
        hostPath:
          path: /mnt/transmission/watch
          type: DirectoryOrCreate
      - name: downloads
        persistentVolumeClaim:
          claimName: downloads-pvc