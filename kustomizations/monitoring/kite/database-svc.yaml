apiVersion: v1
kind: Service
metadata:
  name: kite-postgresql
  labels:
    app.kubernetes.io/name: kite-postgresql
spec:
  type: ClusterIP
  ports:
  - port: 5432
    targetPort: 5432
  selector:
    app.kubernetes.io/name: kite-postgresql
