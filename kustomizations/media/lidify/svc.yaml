apiVersion: v1
kind: Service
metadata:
  name: lidify
  annotations:
    external-dns.alpha.kubernetes.io/hostname: lidify.lan
spec:
  selector:
    app: lidify
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
  type: LoadBalancer
  loadBalancerIP: 192.168.1.28