apiVersion: v1
kind: Service
metadata:
  name: wikijs
  labels:
    app.kubernetes.io/name: wikijs
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: wikijs
  ports:
  - name: http
    port: 3000
    targetPort: 3000
    nodePort: 30300
