apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# see https://docs.ntfy.sh/install/#__tabbed_9_6
resources:
  - deployment.yaml 
  - svc.yaml
  - pvc.yaml
  - ingress.yaml
  
configMapGenerator:
    - name: server-config
      files: 
        - server.yml