apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

generatorOptions:
  disableNameSuffixHash: true

secretGenerator:
  - name: frigate-templates
    files:
      - templates/config.yml.tpl

resources:
- sa.yaml
- deployment.yaml
- svc.yaml
- ingress.yaml

namespace: home-assistant
