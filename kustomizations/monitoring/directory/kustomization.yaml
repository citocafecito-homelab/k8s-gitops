apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- daemonset.yaml

generatorOptions:
  disableNameSuffixHash: true

configMapGenerator:
- name: folder-script
  files:
  - scripts/exporter.sh