apiVersion: v1
kind: Service
metadata:
  name: vault
spec:
  type: NodePort
  selector:
    app: vault
  ports:
  - name: http
    protocol: TCP
    port: 8200
    targetPort: 8200
    nodePort: 30200

# apiVersion: networking.k8s.io/v1
# kind: Ingress
# metadata:
#   name: vault
#   annotations:
#     nginx.ingress.kubernetes.io/backend-protocol: "HTTPS"
# spec:
#   ingressClassName: nginx
#   rules:
#   - host: vault.catuai.lan
#     http:
#       paths:
#       - path: /
#         pathType: Prefix
#         backend:
#           service:
#             name: vault
#             port:
#               number: 8200
# tls:
# - hosts:
#   - vault.catuai.lan
#   secretName: vault-tls
