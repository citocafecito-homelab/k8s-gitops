# Command: helm upgrade --install prometheus . -n monitoring -f values.yaml
apiVersion: v2
name: prometheus
description: Stack de monitoreo manual con dependencias oficiales
type: application
version: 0.1.0
appVersion: "2.10.1"

dependencies:
  - name: kube-prometheus-stack
    version: "84.0.0"
    repository: "https://prometheus-community.github.io/helm-charts"