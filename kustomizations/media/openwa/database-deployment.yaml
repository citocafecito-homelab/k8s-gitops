apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: openwa-postgresql
spec:
  serviceName: openwa-postgresql
  replicas: 1
  selector:
    matchLabels:
      app: openwa-postgresql
      app.kubernetes.io/name: openwa-postgresql
  template:
    metadata:
      labels:
        app: openwa-postgresql
        app.kubernetes.io/name: openwa-postgresql
    spec:
      serviceAccountName: openwa
      containers:
      - name: postgres
        image: postgres:18
        ports:
        - containerPort: 5432
        envFrom:
        - configMapRef:
            name: openwa-database
        volumeMounts:
        - name: openwa-postgresql-storage
          mountPath: /var/lib/postgresql
        livenessProbe:
          exec:
            command: [ "pg_isready", "-U", "openwa" ]
          initialDelaySeconds: 20
          periodSeconds: 30
          failureThreshold: 3
        readinessProbe:
          exec:
            command: [ "pg_isready", "-U", "openwa" ]
          initialDelaySeconds: 10
          periodSeconds: 15
          failureThreshold: 3
        resources:
          requests:
            cpu: 50m
            memory: 128Mi
      nodeSelector:
        kubernetes.io/hostname: catuai
      tolerations:
      - key: node-role.kubernetes.io/control-plane
        operator: Exists
        effect: NoSchedule
      volumes:
      - name: openwa-postgresql-storage
        persistentVolumeClaim:
          claimName: openwa-postgresql-pvc
