apiVersion: apps/v1
kind: Deployment
metadata:
  name: navidrome
  labels:
    app: navidrome
spec:
  replicas: 1
  selector:
    matchLabels:
      app: navidrome
      app.kubernetes.io/name: navidrome
  template:
    metadata:
      labels:
        app: navidrome
        app.kubernetes.io/name: navidrome
    spec:
      containers:
      - name: navidrome
        image: deluan/navidrome:latest
        ports:
        - containerPort: 4533
          name: http
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 500m
            memory: 1Gi
        envFrom:
          - configMapRef:
              name: navidrome-config
        env:
          - name: ND_LASTFM_APIKEY
            valueFrom:
              secretKeyRef:
                name: navidrome-lastfm-secret
                key: ND_LASTFM_APIKEY
          - name: ND_LASTFM_SECRET
            valueFrom:
              secretKeyRef:
                name: navidrome-lastfm-secret
                key: ND_LASTFM_SECRET
        volumeMounts:
        - name: cache
          mountPath: /data/cache
        - name: navidrome-data
          mountPath: /data
        - name: navidrome-backup
          mountPath: /backup
        - name: navidrome-music
          mountPath: /music
        - name: playlists
          mountPath: /music/playlists
      nodeSelector:
        kubernetes.io/hostname: caturra
      volumes:
      - name: cache
        emptyDir:
          medium: Memory
          sizeLimit: 750Mi
      - name: navidrome-data
        hostPath:
          path: /mnt/navidrome
          type: DirectoryOrCreate
      - name: navidrome-backup
        hostPath:
          path: /mnt/navidrome/backup
          type: DirectoryOrCreate
      - name: navidrome-music
        hostPath:
          path: /mnt/music
          type: DirectoryOrCreate
      - name: playlists
        configMap:
          name: shared-playlists